# Spring properties
spring:
  application:
     name: simple-client-service

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://nameserver:1111/eureka/
  instance:
    hostname: simpleclient
    preferIpAddress: false

# HTTP Server
server:
  port: 2222   # HTTP (Tomcat) port

ribbon:
  eureka:
    enabled: true
